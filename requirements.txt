# Playbook核心依赖
# 系统和网络相关
# 模板引擎
# HTTP请求（健康检查需要）
# 可能的其他依赖包（如果使用到的话）
click>=8.0.0
rich>=13.0.0
PyYAML>=6.0

# SSH / file transfer / system
paramiko>=3.0.0
psutil>=5.9.0
scp>=0.14.4  # optional: required only if you use SCP uploads (pip install scp)

# Utilities
python-dateutil>=2.8.0
jinja2>=3.1.0
requests>=2.28.0

# Data / reporting
pandas>=1.5.0

# Optional / commented packages - enable if used in your environment
# docker>=6.0.0
# jsonschema>=4.0.0

# Developer/test helpers (optional)
# pytest>=7.0.0

# Benchmark 依赖
aiohttp>=3.8.0
tqdm>=4.64.0
numpy>=1.23.0
openpyxl>=3.0.0
transformers>=4.30.0
