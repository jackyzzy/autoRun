# Service示例
# 暴露应用服务

apiVersion: v1
kind: Service
metadata:
  name: example-service
  labels:
    app: example-app
spec:
  type: ClusterIP  # 或 NodePort, LoadBalancer
  selector:
    app: example-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
